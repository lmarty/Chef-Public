import os.path

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '<%= @db_name %>',
        'USER': '<%= @db_user %>',
        'PASSWORD': '',
        'HOST': '/var/run/postgresql'
    }
}

# use secret key autogenerated during chef run
with open(os.path.abspath(os.path.join(
               __file__, '..', '..', '.secret_key'))) as f:
  SECRET_KEY = f.read().strip('\n')

DEBUG = <%= @debug %>

# SECURITY: change this to allowed fqdn's to prevent host poisioning attacks
# see https://docs.djangoproject.com/en/1.5/ref/settings/#std:setting-ALLOWED_HOSTS
ALLOWED_HOSTS = ['*']

# use Chef configuration management module
CM_MODULE = 'cm.chef'

# use real provider modules
PROVIDER_MODULES = ('mock', 'digitalocean', 'ec2', 'rackspace', 'static')
