# generated by your mom
127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6

# needed for DB import 
10.0.0.172 ci.pp.local

<% @localhostentry.each do |entry| -%>
<%= entry['ipv4addr'] %> <%= entry['fqdn'] %> <%= entry['fqdn'].split('.')[0] %>
<% end -%>

<% @nodeentries.each do |entry| -%>
<%= entry['ipv4addr'] %> <%= entry['fqdn'] %> <%= entry['fqdn'].split('.')[0] %>
<% end -%>
